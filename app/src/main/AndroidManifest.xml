<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rider_atrafficsolution">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RiderATrafficSolution">
        <activity android:name=".DriverCollectCashActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DriverInitialSetLocationActivity"
            android:label="@string/title_activity_driver_initial_set_location" />
        <activity
            android:name=".UserSideDriverLocationUpdateActivity"
            android:label="@string/title_activity_user_side_driver_location_update" />
        <activity
            android:name=".TestMapsActivity"
            android:label="@string/title_activity_test_maps" />
        <activity android:name=".HistoryShowingActivity" />
        <activity android:name=".DriverReceiveRequestActivity" />
        <activity
            android:name=".DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/Theme.RiderATrafficSolution.NoActionBar" />
        <activity android:name=".WaitingActivity" />
        <activity
            android:name=".RequestConfirmActivity"
            android:label="@string/title_activity_request_confirm" />
        <activity android:name=".CarBikeSearchActivity" />
        <activity android:name=".DriverVehicleTypeSelection" />
        <activity
            android:name=".DriverLocationUpdate"
            android:label="@string/title_activity_driver_location_update" />
        <activity android:name=".BusJourneyCompleteActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ShowBusLoationActivity"
            android:label="@string/title_activity_show_bus_loation" />
        <activity android:name=".BusSeatSelection" />
        <activity android:name=".ChooseVehicleActivity" />
        <activity android:name=".EmailAuthActivity" />
        <activity android:name=".PhoneAuthActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>